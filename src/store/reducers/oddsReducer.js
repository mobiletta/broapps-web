/* import { LOAD_DATA_SUCCESS } from '../actions/oddsActions';

const initialState = {
  odds: [],
};

export default function reducer(state = initialState, action) {
  switch (action.type) {
    case LOAD_DATA_SUCCESS: {
      return {
        ...state,
        odds: action.data,
      };
    }
    default: {
      return state;
    }
  }
}
 */
